import React from "react";
import ReferralPage from "./_components/ReferralPage";

export const metadata = {
  title: "Zikoro Credentials - Refer",
  description: "Refer your friends to Zikoro Credentials",
};

const page = () => {
  return <ReferralPage />;
};

export default page;
